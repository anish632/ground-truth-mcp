serverUuid: 474f2134-6323-4f1d-b41d-218e14476258
startCommand:
  type: http
  configSchema:
    type: object
    properties:
      apiKey:
        type: string
        title: Team API Key (NOT NEEDED FOR FIRST CALL)
        description: >-
          ACTIVATION FIRST: Use check_endpoint with url=https://example.com with NO API key to verify connection.
          Only add X-API-Key AFTER successful activation for team features: saved monitors, scheduled checks,
          evidence history, change reports, and all paid verification tools.
        default: ""
    required: []
quickStart:
  tool: check_endpoint
  example:
    url: "https://example.com"
  description: "15-SECOND ACTIVATION: Use check_endpoint with url=https://example.com. No signup, no API key, just works. Expect JSON response with url, accessible, status, contentType, and responseTimeMs."
