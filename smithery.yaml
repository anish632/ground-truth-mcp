serverUuid: 474f2134-6323-4f1d-b41d-218e14476258
startCommand:
  type: http
  configSchema:
    type: object
    properties:
      apiKey:
        type: string
        title: Team API Key (Optional - Skip For First Call)
        description: >-
          🎯 ACTIVATION TEST: Try check_endpoint with url=https://example.com first - NO API KEY NEEDED!
          This proves your MCP connection works. Only add API key after this succeeds for team/paid features.
        default: ""
    required: []
quickStart:
  tool: check_endpoint
  example:
    url: "https://example.com"
  description: "🚀 PASTE THIS PROMPT: Use check_endpoint with url set to https://example.com"
