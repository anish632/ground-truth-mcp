serverUuid: 474f2134-6323-4f1d-b41d-218e14476258
startCommand:
  type: http
  configSchema:
    type: object
    properties:
      apiKey:
        type: string
        title: Team API Key
        description: >-
          Optional X-API-Key for team-plan access to paid verification tools.
          Leave empty for the first check_endpoint call with url=https://example.com.
          Add only after the first call works.
        default: ""
    required: []
