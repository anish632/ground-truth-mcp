serverUuid: 474f2134-6323-4f1d-b41d-218e14476258
startCommand:
  type: http
  configSchema:
    type: object
    properties:
      apiKey:
        type: string
        title: Team API Key
        description: >-
          Optional X-API-Key for team-plan access to paid verification tools
          (check_pricing, compare_pricing_pages, assess_compliance_posture,
          compare_competitors, verify_claim, estimate_market, test_hypothesis).
          Leave empty for the first free tool call with check_endpoint or
          inspect_security_headers, or for agentic x402 pay-per-use.
        default: ""
    required: []
